<!DOCTYPE html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7"> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8"> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9"> <![endif]-->
<!--[if gt IE 8]><!--> <html class="no-js"> <!--<![endif]-->
    <head>
        <meta charset="utf-8">

        <title>{{ repository.full_name }} - gitscore</title>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
        <meta name="description" content="Checkout who's the kick-ass developer in {{ repository.repo }}" />
        <link rel="stylesheet" href="/assets/dev/css/app.css" />

        {% include 'webapp.html' %}
    </head>
    <body>
        <h1 style="text-align:center">{{ repository.full_name }}</h1>

        <ul>
            {% for user in users %}
                <li>
                    <img src="{{ user.avatar_url }}" width="48px" height="48px" />
                    <br />
                    user: <a href="/{{ repository.full_name }}/{{ user.login }}">{{ user.login }}</a>
                    <br />
                    score: {{ user.score }} points
                    <br />
                    quality: {{ user.quality|round(1) }}%
                    <br />
                    pulls: {{ user.pulls }}
                    <br />
                    bounces: {{ user.bounces }}
                    <br />
                    reviews: {{ user.reviews }}
                </li>
            {% endfor %}
        </ul>
    </body>
</html>